---
title: How it works
---

- similar api/concepts for each adapter but each is different to account for host specific features
- request handler (route requests through vite to the handler)
    - dev
    - ssg
- ssr: entrypoint. can be the same or distinct server entrypoint
- ssg: use prerender entrypoint to give you control

http ftw
