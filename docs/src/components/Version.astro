---
import { getLatestNPMVersion } from "../utils/get-latest-npm-version";

export interface Props {
  pkgName: string;
}

const version = await getLatestNPMVersion(Astro.props.pkgName);
---

<span>v{version}</span>
